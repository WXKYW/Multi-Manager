# Keep the application version in package.json aligned with main merges.
# The merge commit is bumped in a follow-up commit, then the release pipeline
# is dispatched explicitly because GITHUB_TOKEN pushes do not trigger it.
name: Bump Main Version

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    types: [closed]

permissions:
  contents: write
  actions: write

concurrency:
  group: version-bump-main
  cancel-in-progress: false

jobs:
  bump:
    if: >-
      (github.event_name == 'push' && github.actor != 'github-actions[bot]' &&
      !contains(github.event.head_commit.message, 'chore(release): bump version')) ||
      (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout full history
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.2.2
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.merge_commit_sha || github.sha }}

      - name: Detect merge commit
        id: merge
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          EVENT_NAME: ${{ github.event_name }}
          MERGE_COMMIT_SHA: ${{ github.event.pull_request.merge_commit_sha }}
        shell: bash
        run: |
          if [ "$EVENT_NAME" = "pull_request" ]; then
            PARENT_COUNT=$(git rev-list --parents -n 1 "$MERGE_COMMIT_SHA" | awk '{print NF - 1}')
            echo "parent_count=${PARENT_COUNT}" >> "$GITHUB_OUTPUT"
            echo "base_revision=${BASE_SHA}" >> "$GITHUB_OUTPUT"
            if [ "$PARENT_COUNT" -ge 2 ]; then
              # The push event handles regular merge commits. PR events handle
              # squash and rebase merges, whose resulting commits have one parent.
              echo "is_merge=false" >> "$GITHUB_OUTPUT"
            else
              echo "is_merge=true" >> "$GITHUB_OUTPUT"
            fi
            exit 0
          fi

          PARENT_COUNT=$(git rev-list --parents -n 1 HEAD | awk '{print NF - 1}')
          echo "parent_count=${PARENT_COUNT}" >> "$GITHUB_OUTPUT"
          echo "base_revision=HEAD^1" >> "$GITHUB_OUTPUT"
          if [ "$PARENT_COUNT" -ge 2 ]; then
            echo "is_merge=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_merge=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Resolve next version
        if: steps.merge.outputs.is_merge == 'true'
        id: version
        env:
          BASE_REVISION: ${{ steps.merge.outputs.base_revision }}
        shell: bash
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          BASE_VERSION=$(git show "${BASE_REVISION}:package.json" | node -e "let input=''; process.stdin.on('data', chunk => input += chunk); process.stdin.on('end', () => console.log(JSON.parse(input).version));")

          if node -e "const a='$CURRENT_VERSION'.split('.').map(Number); const b='$BASE_VERSION'.split('.').map(Number); process.exit(a[0] > b[0] || (a[0] === b[0] && (a[1] > b[1] || (a[1] === b[1] && a[2] > b[2]))) ? 0 : 1)"; then
            VERSION="$CURRENT_VERSION"
            CHANGED=false
          else
            npm version patch --no-git-tag-version --ignore-scripts
            VERSION=$(node -p "require('./package.json').version")
            CHANGED=true
          fi

          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "changed=${CHANGED}" >> "$GITHUB_OUTPUT"

      - name: Commit version bump
        if: steps.merge.outputs.is_merge == 'true' && steps.version.outputs.changed == 'true'
        env:
          ACTOR: ${{ github.actor }}
          ACTOR_ID: ${{ github.actor_id }}
          VERSION: ${{ steps.version.outputs.version }}
        shell: bash
        run: |
          git config user.name "$ACTOR"
          git config user.email "${ACTOR_ID}+${ACTOR}@users.noreply.github.com"
          git add package.json package-lock.json
          git commit -m "chore(release): bump version to v${VERSION} [skip ci]"
          git push origin HEAD:main

      - name: Dispatch release for main version
        if: steps.merge.outputs.is_merge == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: >-
          gh workflow run release.yml --ref main
          --field version="$VERSION"
          --field prerelease=false
